#include<Stdio.h>
int main(){
    for (int i = 0; i < 5;i++){ //vòng lặp chạy 5 lần, mỗi lần lặp phép toán ko đổi --> độ phức tạp vòng lặp là O(5)
        printf("hello\n");
    }
    return 0;
}
//--> o(n)